<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cdsscheduleapp_legacy">
    <!-- 인터넷 사용을 위한 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.cdsscheduleapp_legacy.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name="com.example.cdsscheduleapp_legacy.RoutineActivity" />
        <activity android:name="com.example.cdsscheduleapp_legacy.MemoActivity" />
        <activity android:name="com.example.cdsscheduleapp_legacy.SettingsActivity" />
        <activity android:name="com.example.cdsscheduleapp_legacy.CalendarActivity" /> <!-- play services ads에 전달할 앱 ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />

        <activity android:name="com.example.cdsscheduleapp_legacy.ToDoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>